[project]
name = "visual-agent-backend"
version = "1.0.0"
description = "Production FastAPI Backend & WebSocket Gateway with Clean Architecture"
readme = "README.md"
requires-python = ">=3.10"

[tool.pytest.ini_options]
minversion = "8.0"
addopts = "-ra -q --asyncio-mode=auto"
testpaths = ["tests"]
